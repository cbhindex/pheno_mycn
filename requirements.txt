# Core Pheno-MYCN dependencies (model, training, and inference).
#
# Version ranges below reflect the APIs the code actually uses:
#   * pytorch-lightning 1.x Trainer API (`gpus=`, `*_epoch_end` hooks)
#   * torchmetrics >= 0.11 (the `task=` classification API)
# Tested stack: torch 2.0.1 (cu118), pytorch-lightning 1.9.5, torchmetrics 0.11.4,
# numpy 1.26.4. For training, use torch 2.0.x (the Lightning 1.x line targets it);
# install the CUDA build matching your driver from https://pytorch.org.

torch>=1.12,<2.1
torchvision>=0.13
pytorch-lightning>=1.6,<2.0
torchmetrics>=0.11,<1.0
setuptools<81                  # PL 1.x imports pkg_resources, removed in setuptools 81
nystrom-attention>=0.0.9       # required by the TransMIL baseline
numpy>=1.21,<2                 # the pinned torch/Lightning stack expects numpy 1.x
pandas>=1.3
scikit-learn>=1.0
pyyaml>=5.4
addict>=2.4

# --- The plug-and-play inference tool only needs `torch` + `numpy` + `pyyaml` +
#     `addict` (i.e. `pip install -e .`) and works with newer torch too. The
#     packages above are required for (re)training and the full Lightning pipeline.
